const postLike = () => {
  // const like = useMutation<void, Error>({
  //   mutationFn: (postId: number) =>
  //     axios
  //       .post(
  //         `/api/post/like`,
  //         { postId },
  //         {
  //           headers: {
  //             Authorization: `bearer ${localStorage.getItem("token")}`,
  //           },
  //         }
  //       )
  //       .then((res) => res.data),
  //       onMutate
  // });
};

export default postLike;
